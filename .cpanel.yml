---
# groundwork-web.com — deploy target: ~/public_html/
# Git repo: ~/repositories/offer1/ (source only)
# Live site: https://groundwork-web.com

deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/
    - export APPROOT=$HOME
    - /bin/mkdir -p $APPROOT/storage
    - /bin/cp -f public/index.php $DEPLOYPATH
    - /bin/cp -f public/robots.txt $DEPLOYPATH
    - /bin/cp -f public/sitemap.xml $DEPLOYPATH
    - /bin/cp -f public/.htaccess $DEPLOYPATH
    - /bin/cp -R public/css $DEPLOYPATH
    - /bin/cp -R public/js $DEPLOYPATH
    - /bin/cp -R public/images $DEPLOYPATH
    - /bin/rm -rf $APPROOT/config $APPROOT/lib $APPROOT/includes
    - /bin/cp -R config $APPROOT/
    - /bin/cp -R lib $APPROOT/
    - /bin/cp -R includes $APPROOT/
    - if [ ! -f $APPROOT/.env ]; then /bin/cp .env.example $APPROOT/.env; fi
    - /bin/rm -f $DEPLOYPATH/parking-page.shtml
    - /bin/chmod 775 $APPROOT/storage
